# 复制为 .env 后按本机修改（不要提交 .env 若含内网信息）
# Windows：在「本目录」PowerShell 执行 Copy-Item .env.example .env

# WebRTC 对外宣告地址。本机浏览器可暂用 127.0.0.1；手机同 WiFi 访问时请改为电脑局域网 IP，例如：
# SRS_CANDIDATE=192.168.1.33
SRS_CANDIDATE=127.0.0.1
